version: "3.8"

services:
  redis:
    image: redis:latest
    ports:
      - 6379:6379
    command: redis-server --save 20 1 --loglevel warning
    volumes:
      - ./storage/redis/data:/data

volumes:
  redis:
    driver: local
